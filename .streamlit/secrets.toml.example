# =============================================================================
# Streamlit Secrets Configuration for CareerLens
# =============================================================================
# Copy this file to secrets.toml and fill in your API keys
# For Streamlit Cloud: Add these secrets in the app settings dashboard
#
# IMPORTANT: Never commit secrets.toml to version control!
# =============================================================================

# Azure OpenAI Configuration
# Include the /openai suffix - the app handles it correctly
AZURE_OPENAI_ENDPOINT = "https://hkust.azure-api.net/openai"
AZURE_OPENAI_API_KEY = "your-azure-openai-api-key"

# =============================================================================
# IMPORTANT: DEPLOYMENT NAMES - COMMON CAUSE OF 404 ERRORS!
# =============================================================================
# These MUST match the EXACT deployment names in your Azure/APIM configuration!
# 
# For HKUST APIM, the deployment names may be different from standard names.
# Check with HKUST IT or the Azure Portal for the correct deployment names.
#
# Common deployment names to try if you get 404 errors:
#   - "gpt-4", "gpt-4o", "gpt-4o-mini"
#   - "gpt-35-turbo" (NOT "gpt-3.5-turbo")
#   - Custom names like "hkust-gpt4", "gpt4-deployment"
#
# To diagnose 404 errors, run: python tests/test_hkust_endpoint.py YOUR_API_KEY
# =============================================================================
AZURE_OPENAI_DEPLOYMENT = "gpt-4o-mini"
AZURE_OPENAI_EMBEDDING_DEPLOYMENT = "text-embedding-3-small"

# API Version (optional - defaults to 2024-02-15-preview)
# AZURE_OPENAI_API_VERSION = "2024-02-15-preview"

# RapidAPI Configuration (for job search)
RAPIDAPI_KEY = "your-rapidapi-key"

# Pinecone Configuration (optional - for vector search)
PINECONE_API_KEY = "your-pinecone-api-key"
